# Same-origin API proxy (Next.js rewrites /api → backend). Override for direct URL if needed.
NEXT_PUBLIC_API_URL=/api
BACKEND_URL=http://127.0.0.1:8000
NEXT_PUBLIC_TENANT_ID=00000000-0000-0000-0000-000000000001
